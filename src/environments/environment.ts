import { Environment } from './environment.model';

export const environment: Environment = {
  production: true,
  environmentName: 'production',
  apiUrl: 'https://timely-florentine-2f5598.netlify.app/assets/items',
  enableLogging: false
};
